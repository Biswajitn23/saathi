# API Configuration
# Leave blank to use localStorage only, or provide a backend API URL for production

VITE_API_URL=http://localhost:3000/api

# Optional: MongoDB Atlas Connection (for backend setup)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/saathi?retryWrites=true&w=majority

# Optional: Firebase Configuration (alternative to MongoDB)
# FIREBASE_PROJECT_ID=your-project-id
# FIREBASE_API_KEY=your-api-key
